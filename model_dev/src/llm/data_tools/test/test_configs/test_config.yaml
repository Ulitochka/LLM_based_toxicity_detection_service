
model_config:
  model: google/gemma-2-2b-it
  quant8: True
  quant4: False
  token: ${HF_TOKEN}
  peft: True

data_config:
  task_type: cls_toxic
  prefix: 'токсичный текст: '
  collator_type: chat
  max_target_len: 5
  max_input_len: 1024
  per_device_inference_batch_size: 20
